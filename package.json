{
  "name": "node-mongo",
  "version": "1.0.0",
  "description": "Node.js - MongoDB - express",
  "main": "dist/src/server.js",
  "author": "Fanis Prodromou",
  "scripts": {
    "start": "node dist/src/server.js",
    "ts_start": "NODE_ENV=development yarn run build:live",
    "postinstall": "yarn build",
    "win_start": "set NODE_ENV=development  && yarn run build:live",
    "test": "mocha --reporter spec --require ts-node/register test/**/*.test.ts",
    "build:live": "nodemon --exec ./node_modules/.bin/ts-node -- ./src/server.ts",
    "build": "rm -rf dist && tsc"
  },
  "license": "MIT",
  "private": true,
  "dependencies": {
    "body-parser": "^1.18.2",
    "cors": "^2.8.4",
    "dotenv": "^5.0.0",
    "express": "^4.16.2",
    "mongoose": "^5.0.4",
    "tslint": "^5.9.1"
  },
  "devDependencies": {
    "@types/body-parser": "^1.16.8",
    "@types/cors": "^2.8.3",
    "@types/express": "^4.11.1",
    "@types/mongodb": "^3.0.5",
    "@types/mongoose": "^5.0.2",
    "@types/node": "^9.4.5",
    "nodemon": "^1.17.1",
    "ts-node": "^4.1.0",
    "tslint-config-airbnb": "^5.6.0",
    "typescript": "^2.7.1"
  }
}
